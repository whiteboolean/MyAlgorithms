package com.example.myalgorithms.bilibili_tuling;

/**
 * 给定两个字符串 A、B，判断B在A中是否存在，存在返回A中的下标，不存在返回-1
 *
 * 例： A: ABCABCAABCABCD
 *     B: ABCABCD
 * 返回值：7
 *
 * KMP:
 * 解决模式串在文本串是否出现过
 *  前缀：字符串A和B，A=B+S,S非空，则B为A的前缀
 *  后缀：A=S+B,S非空，则B为A的后缀
 *  PMT值：前缀集合和后缀集合的交集中，最长元素的长度
 *
 */
public class P45_字符串搜索_KMP算法 {

    //kmp
    //前缀：
}
